#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{	printf("with proper grammar, but the outcome is a piece of art,\n"); /* 'printf' displays the text in double quotes onto the standard output */
		return (0);
}
