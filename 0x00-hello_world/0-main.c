#include <stdio.h>
/**
 * main - Entry point
 *
 * a program that assembles the intel synta
 * a program that assembles the intel syntax
 * Return: Always 0 (Success)
 */
int main(void)
{
    return (0);
}

