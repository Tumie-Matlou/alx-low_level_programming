#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{	puts("\"Programming is like building a multilingual puzzle"); /* 'puts' prints the text in  double quotes on the standard output */
		return (0);
}
